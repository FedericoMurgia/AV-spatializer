; AV Spatializer, classic ESP32 with BLE MIDI output, no screen.
; Reference config.

[env:esp32]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
lib_deps =
    fastled/FastLED
    h2zero/NimBLE-Arduino@^1.4.0
